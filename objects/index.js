/*const game = {
  title: 'guess the number',
  biggestNum: 100,
  play: function() {
    const randomNumber = Math.floor(Math.random() 
        * (this.biggestNum - this.smallestNum + 1)
        + this.smallestNum);
    this.secretNum = randomNumber;
  }
    )
  }
}
const person = {
    ... name: 'chris',
    ... hat: 'yellow',
    ...
}
person.name;*/

